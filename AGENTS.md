# Codex SDD guidance

This file is an SDD lifecycle guidance target. Generated agent guidance is a
projection over `.fsgg/agents.yml` and readiness data; it is not a second source
of truth.

## Project board

The canonical work board for every issue owned by `EHotwagner/S.I.R.` is the
user-owned GitHub Project **EHotwagner / S.I.R.** (project 6), not the FS-GG
organization Coordination project. Cross-repository producer issues remain on
the owning producer's coordination board; reference them from S.I.R. through
`Blocked by:` links instead of adding the producer issue to the S.I.R. board.
